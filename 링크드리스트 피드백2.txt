수정한 코드에 대해서 diff from previous ver해서 어디를 어떻게 고쳤는지 볼 수 있음. 백문이 불여일견이라고 한번 해볼것.

LinkedList.cpp에서

파괴자 함수 : ::연산자는 자기 클래스 내부의 함수에 대해서는 쓸 필요가 없다...기보단 쓰지 않음. 다른 클래스의 함수를 쓸 때 ::를 붙인다. 현재 자기 클래스의 함수에 대해서는 this포인터를 쓰고, ::을 쓰는 함수는 아마 Static함수에 국한될 것이다.

add() : index>length, index<0에서 모든 예외가 처리될거라고 봄. head==NULL은 쓸데없는 처리로 보임.
이 함수를 public으로 할 이유가 없을 것 같기도 함....
add를 index가 아니라 node를 받는 걸로 고쳐야겠다.

addLast() : find()도 파괴자에서 썼던 거와 동일하게 ::를 쓰지는 않음.

find() : 이것도 node*리턴하는걸로 고쳐보자.

printIndex()는 대체...

나는 함수 이름 쓸 때 첫 글자 대문자 띄어쓰기 첫 글자 대문자,
변수 이름 쓸 때 첫 글자 소문자 띄어쓰기 대문자로 씀. 
헝가리안 표현법이라고 타입과 리턴값까지 변수 함수 이름에 다 넣는 방식도 있지만 나는 별로 좋아하지 않음
ex ) bool* lpbIsEmpty 같은 식. 괜히 길어져서 줘깥음. 멤버변수나 전역변수에는 가끔 쓰는 사람 있음.

doubley liked list로 고쳐보자
index가 아니라 node*를 주고받는 방식으로 고칠 것.




